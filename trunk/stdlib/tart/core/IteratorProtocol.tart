/** The iterator protocol. An iterator has a 'next' method which returns
    either the next value in the sequence, or 'Nothing' to indicate the
    end of the sequence.
*/
protocol IteratorProtocol[%T] {
  def next -> T or void;
}

//interface Iterator[%T] {
//  typealias Protocol = IteratorProtocol;
//  typealias Finished = Finished;
//}
