/** Map interface */
interface Map[%Key, %Value] : Iterable[Entry] {
  struct Entry {
    var key:Key;
    var value:Value;
  }

  /** Access a value by key. */
  def [key:Key]:Value { get; set; }
  
  /** The number of items in the map. */
  def length:int { get; }
}
