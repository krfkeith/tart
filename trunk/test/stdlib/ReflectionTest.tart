import tart.reflect.Module;

@EntryPoint
def main(args:String[]) -> int {
  testModuleReflection();
  testModuleList();
  return 0;
}

def testModuleReflection() {
  let m = Module.thisModule();
  Debug.assertEq("ReflectionTest", m.name);
}

def testModuleList() {
  for i = 0; i < Module.moduleCount; ++i {
    Debug.writeLn("module");
    //Debug.writeLn(Module.moduleList[i].name);
  }
  //for mod in Module.modules() {
  //  Debug.writeLn(mod.name);
  //}
}
