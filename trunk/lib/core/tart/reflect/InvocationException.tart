/** Exception which is thrown when the attempt to call a method via reflection
    has the incorrect number of arguments. */
class InvocationException : Exception {
  def construct(msg:String) {
    super(msg);
  }
}
