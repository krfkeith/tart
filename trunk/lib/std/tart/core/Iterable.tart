import tart.annex.Essential;

/** An iterface for iterable collections. */
@Essential interface Iterable[%T] {
  /** Return an iterator object for this collection. */
  readonly def iterate -> Iterator[T];
}
